name: Build Termux App

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew

    - name: Build with Gradle for arm64-v8a
      env:
        TERMUX_PACKAGE_VARIANT: apt-android-7
      run: ./gradlew :app:assembleDebug -PtermuxArch=arm64-v8a

    # --- НАШ НОВЫЙ ШАГ ДЛЯ ОТЛАДКИ ---
    - name: List files in build outputs
      run: ls -R app/build/outputs/
    # --- КОНЕЦ НОВОГО ШАГА ---

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: termux-arm64-debug.apk
        # Мы пока оставляем старый путь. После следующей сборки мы заменим его на правильный.
        path: app/build/outputs/apk/debug/app-arm64-v8a-debug.apk
